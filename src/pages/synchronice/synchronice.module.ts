import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { SynchronicePage } from './synchronice';

@NgModule({
  declarations: [
    //SynchronicePage,
  ],
  imports: [
    IonicPageModule.forChild(SynchronicePage),
  ],
})
export class SynchronicePageModule {}
